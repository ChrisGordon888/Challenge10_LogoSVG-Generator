{
  "name": "challenge10",
  "version": "1.0.0",
  "description": "I will build a node.js command-line application that takes user input to generate a logo and save it as a SVG file.",
  "main": "index.js",
  "type": "module",

  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "inquirer": "^9.2.0",
    "svg-captcha": "^1.4.0"
  }
}
